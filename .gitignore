# ── Python ────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python

# ── Virtual environment (this directory IS the venv) ──────────────────────
bin/
lib/
lib64
include/
share/
pyvenv.cfg
CACHEDIR.TAG
.lock

# ── Jupyter ───────────────────────────────────────────────────────────────
.ipynb_checkpoints/

# ── LaTeX build artefacts ─────────────────────────────────────────────────
*.aux
*.log
*.out
*.toc
*.bbl
*.blg
*.synctex.gz
*.fls
*.fdb_latexmk

# ── OS / editor ───────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.vscode/
.idea/

# ── Installed packages inside the repo (text-flappy-bird-gym) ─────────────
# Tracked separately — keep the subdir but ignore any compiled artefacts
text-flappy-bird-gym/**/__pycache__/
text-flappy-bird-gym/**/*.egg-info/